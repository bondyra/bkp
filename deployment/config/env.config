# kubernetes workspace
export WORKSPACE=test

# kubernetes VM ports on which services are published
export SCHEMA_REGISTRY_PORT=30801
export CONTROL_CENTER_PORT=30921
export CONNECT_PORT=30808
export ELASTICSEARCH_PORT=32000

# the kafka topic name
# TODO: the intent is to have pluggable data sources, some generic mechanism is a must
export TOPIC_NAME=test

# kafka connect connector submission configuration
export CONNECT_PV_CLAIM_NAME="connect-plugins-pv-claim"
export CONNECT_PLUGIN_PATH="/usr/share/confluent-hub-components"

export KAFKA_MAINTENANCE_POD_NAME=kafka-maintenance
