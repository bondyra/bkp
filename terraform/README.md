This directory contains the infrastructure.
For the elasticity, each relevant "block" in the platform has a NodePort configured.
TODO: there are places where things are still hardcoded (e.g. tf state path).
